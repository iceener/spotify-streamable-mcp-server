/**
 * Spotify MCP prompts registration.
 * Add prompt templates here if needed.
 */

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * Register prompts with the MCP server.
 * Currently no prompts are registered for Spotify MCP.
 */
export function registerPrompts(_server: McpServer): void {
  // No prompts registered yet
  // Add Spotify-specific prompts here if needed
}
